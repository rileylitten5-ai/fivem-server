name: Deploy to FiveM
on:
  push:
    branches: [main]
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: FTP Deploy
        uses: SamKirkland/FTP-Deploy-Action@v4.3.4
        with:
          server: syd-h01.vibegames.net
          username: user_qlfuf6tc6krv18xlv6ghj1rvn.e5139dd2
          password: ${{ secrets.FTP_PASSWORD }}
          protocol: sftp
          port: 2022
          local-dir: ./
          server-dir: /home/user_qlfuf6tc6krv18xlv6ghj1rvn.e5139dd2/resources/
